---
import type { Venue } from "./Card.astro";
import type { Theme } from "./Card.astro";

interface props {
  title: string;
  body: string;
  description: string;
  venue: Venue;
  theme: Theme;
}
---
